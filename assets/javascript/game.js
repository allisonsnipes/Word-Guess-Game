'use strict'

$(document).ready(function({

  //starts the game
  startGame(;)
  //resets the game
  resetGame();
  //exit game on quit
  quitGame();
}));

//starting defining starting place
let currentQuestion = 0,
  score = 0,
  guessingWords = [

  ];


//butons on the bottom

//resets the game
$(".resetGame").on("click", functino(event) {
  $(".writeup").hide();
});